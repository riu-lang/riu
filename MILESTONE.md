# 里程碑规划（草稿）

> **v1.0** = 语言核心稳定 + 规范定稿 + 基础 SDK 可用。不设硬性日期。
> 小版本落地见 [`notes/`](notes/)。词法 / 语法 / `riu.core` 提议见 [`docs/spec/prop/`](docs/spec/prop/)。stdlib 不进提议与 spec。本文件只写大版本目标。

---

## 方向

### 语言参考

- `c` `c++` `rust` `kotlin` `java` `python` `go` 不分先后
- 主要参考 `rust` `c++` `kotlin`

### 特性

- 明确的使用
    - 知道每一步类型，在干什么
    - 没有隐式转换
    - 布局而不是继承
    - 移动 or 复制
    - 栈 or 堆
- 统一的用法
    - 尽可能少的特例规则
- 简约的代码
    - `kotlin` 思维
- 内存安全
    - 不直接接触指针
    - 内存由编译器管理，用户只关心业务代码，以及把数据放栈还是堆
    - 指针单向转换 `riu -> ptr`
    - 限制外部函数（后期），类 `rust`
- 代码生成代码（后期）
    - 类 `rust`
    - 类 `java` 注解预处理
- 更强大的接口 `draft`
    - 接口一致
    - 像接口
- 文档：md 格式
- 条件编译
- 代码修复建议
- 测试

---

## v1 — 语言核心稳定 + 规范定稿 + 基础 SDK 可用

### 退出

- spec 全部章节 *Open Issues* 清空或显式转入 v1.x / v2
- 调用 ABI / 内存模型冻结
- SDK 公开方法签名稳定
- 已知 blocker 清零

已满足、不再挡退出：spec-default-body / spec-reflect；闭包捕获；模块 `.ud`。

未关闭的语言 / core 项见 [`docs/spec/prop/`](docs/spec/prop/)（状态以提议文件为准）。stdlib 不在表里。

---

## v1.x — 冻结后的增量（草稿，不展开）

- 多目标（Windows 之外）
- SDK / 工具链在 ABI 冻结后的兼容扩展

---

## v2 — （草稿，不展开）

- 异步 / 并发原语
- 包管理与远程依赖
