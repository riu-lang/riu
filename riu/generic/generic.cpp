// Copyright (c) 2026. Yin-Jinlong@github
// MPL-2.0

#include "generic.h"

static_assert(sizeof(generic::StructInstance) >= 1);
static_assert(sizeof(generic::FnInstance) >= 1);
static_assert(sizeof(generic::SubstFrame) >= 1);
