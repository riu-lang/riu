# 附录 A：保留字

> 与 [`src/yux.g4`](../../src/yux.g4) 的关键字 token 一致；冲突以 `.g4` 为准。

## A.1 关键字

| 关键字 | 说明 |
|--------|------|
| `fn`     | 函数声明 |
| `var`    | 可变变量 |
| `val`    | 不可变变量 |
| `struct` | 结构体声明 |
| `if`     | 条件 |
| `else`   | 条件分支 |
| `loop`   | 循环 |
| `break`  | 跳出循环 |
| `continue` | 进入下次迭代 |
| `ret`    | 返回 |
| `extern` | 外部声明 |
| `null`   | 空字面量 |
| `true`   | 布尔真 |
| `false`  | 布尔假 |

> TODO：补全（与 `yux.g4` 同步审校）。

## A.2 预留但未启用

- TODO
