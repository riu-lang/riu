# 2 `Iter<T>` / 泛型 for-in

| | |
|---|---|
| 状态 | 提议 |
| 开 | 2026-09-13 |
| 旧档 | 无。现 `for-in` 只 lower Array / 定长数组 |

## 提议

完整迭代器 spec；Map 等可直接 `for`。SDK 已有的急切 `filter`/`map` 纳入同一套迭代模型（是否改惰性在本提案内决定）。

## 评估

## 决定

## 规范要点

## 落地
