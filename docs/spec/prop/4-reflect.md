# 4 反射补全

| | |
|---|---|
| 状态 | 提议 |
| 开 | 2026-09-13 |
| 旧档 | [DRAFT-spec-reflect.md](../draft/DRAFT-spec-reflect.md)（反射 v1 已落地，勿改） |

## 提议

`Field.type` / `offset`；显式 receiver；`Self::type` / `Self::fields`；`methods` / `variants` 数组填充；`#Reflect` 命名参数（与 #6 注解形态一起定）。旧草案已拒绝的 `#Inline for` / 按字段自动 derive 仍拒绝。

## 评估

## 决定

## 规范要点

## 落地
