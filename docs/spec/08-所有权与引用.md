# §8 所有权与引用

> 本章规范源自 [`DRAFT-所有权与引用.md`](../../DRAFT-所有权与引用.md)；条款随实现稳定逐步从草案迁入。

## §8.1 概述

- TODO：值类别（owned / borrowed / shared handle）。

## §8.2 句柄与 RC 协议

### §8.2.1 Block 结构
- TODO：`{u32 strong, u32 weak, payload}`；哨兵 `0xFFFFFFFF`。

### §8.2.2 strong / weak 计数协议
- TODO：retain / release 配对；哨兵跳过；归零释放。

## §8.3 `Box<T>`

- TODO：构造、复制（retain）、析构（release）。

## §8.4 `Array<T>`

- TODO：句柄共享、`_array_grow`、按值传参共享语义。

## §8.5 `Weak<T>` 与 `upgrade`

- TODO：弱计数、`upgrade(w) Box<T>?`、禁忌（`Weak<T>?`、`Weak ==`）。

## §8.6 借用 `T&`

- TODO：仅参数与局部 `var`；不参与 RC；生命周期约束。

## §8.7 调用 ABI

### §8.7.1 callee-clean retain/release
### §8.7.2 move-return retain 注入

## §8.8 临时值与作用域

- TODO：临时值清单、RC 泄漏检测、fresh 判定。

## §8.9 禁忌一览

- TODO：字段位置 `T&`、返回值 `T&`、`T& &`、`Weak<T>?`、`Weak == / !=`。

## Open Issues

- 草案 §12 迁移完成进度参见 [`所有权与引用-P1实现.md`](../../所有权与引用-P1实现.md)。
