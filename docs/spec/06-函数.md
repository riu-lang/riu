# §6 函数

## §6.1 函数声明

- TODO：`fn name(params) RetType { ... }` / `fn name(...) RetType = expr`。

## §6.2 参数

### §6.2.1 普通参数
### §6.2.2 引用参数 `T&`
- TODO：仅在参数与局部 `var` 位置允许。
### §6.2.3 参数分组 `fnParamGroup`

## §6.3 返回值

- TODO：返回类型、`ret` 语义、堆句柄类型的 move-return。

## §6.4 泛型函数

- TODO：声明 `fn <T> foo(...)`、调用点实例化、隐式类型推断。

## §6.5 调用 ABI 概要

- TODO：callee-clean retain/release（详见 §8）。

## §6.6 外部声明 `extern`

- TODO：与 C/系统库对接。

## Open Issues

- TODO
