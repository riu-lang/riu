# §9 内置类型

## §9.1 `Box<T>`

- TODO：layout、方法、生命周期合约（详见 §8.3）。

## §9.2 `Array<T>`

- TODO：layout、`len/cap/at/push/pop/...` 语义合约、共享与可变性。

## §9.3 `String`

- TODO：不可变；`{ data: Array<u32> }` codepoint 存储；构造仅 `String(buf)`。

## §9.4 `StringBuilder`

- TODO：可变缓冲；`build()` 后重置语义。

## §9.5 `Weak<T>`

- TODO：详见 §8.5。

## §9.6 `Ptr<T>`

- TODO：原始指针，不参与 RC；与 `extern` 互操作。

## §9.7 `Ref<T>`

- TODO：引用包装（如保留）。

## §9.8 数值与 `bool`

- TODO：方法（`to_string` / `to_i32` / ...）。

## Open Issues

- TODO
