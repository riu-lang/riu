# §5 语句与控制流

## §5.1 声明语句

### §5.1.1 `var` 与 `val`
- TODO：可变性、初始化要求、类型推断。

### §5.1.2 声明同时赋值

## §5.2 赋值语句

- TODO：左值形式（变量、字段、索引、`$.field`）。

## §5.3 表达式语句

## §5.4 `if` / `else`

## §5.5 `loop`

- TODO：`break` / `continue` 行为。

## §5.6 `ret` 语句

- TODO：返回值类型匹配、所有权移交语义见 §8。

## §5.7 作用域与变量寿命

- TODO：块作用域、析构顺序（与 §8 联动）。

## Open Issues

- TODO
