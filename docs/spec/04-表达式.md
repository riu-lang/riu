# §4 表达式

## §4.1 求值顺序

- TODO：从左到右；子表达式求值规则；副作用点。

## §4.2 算术运算

- TODO：整型、浮点；溢出、除零行为。

## §4.3 比较与逻辑运算

- TODO：`== != < > <= >=`；`&& ||` 短路；`!`。

## §4.4 取址与解引用

- TODO：`&expr` 取引用；引用的生命周期约束见 §8。

## §4.5 字段与方法访问

- TODO：`obj.field` / `obj.method(...)` / `$.field` 接收者。

## §4.6 调用

- TODO：函数调用、泛型实例化、参数传递语义。

## §4.7 字面量构造

- TODO：数组字面量、结构体构造、字符串字面量。

## §4.8 条件表达式

- TODO：`if` 作为表达式（如适用）。

## Open Issues

- TODO
