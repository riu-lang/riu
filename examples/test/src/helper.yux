; smoke test for `use helper.*`

struct Point {
  x i32
  y i32

  fn Point(x i32, y i32) {
    $.x = x
    $.y = y
  }

  fn sum() i32 = $.x + $.y
}

fn greet() {
  println("hello from helper")
}

fn add42(x i32) i32 = x + 42
