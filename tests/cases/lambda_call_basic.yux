; Phase 2b §4 / §5：lambda 字面量 + 直接调用（IIFE）
; 零捕获场景；fat-ptr ABI captures-leading（spec §5.3）

fn main() {
  ; IIFE：lambda 字面量括起来后立即调用
  var r1 i32 = ((x i32) i32 => x + 1)(5)
  println(r1.to_string())

  ; 多参 + 组糖
  var r2 i32 = ((a, b i32) i32 => a + b)(3, 4)
  println(r2.to_string())

  ret;
}
