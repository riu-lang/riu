use yux.core.*

fn main() {
    ; 测试声明语句（无初始化）
    var a i32
    var b i64
    var c f32
    
    ; 后续赋值
    a = 10
    b = 20
    c = 3.14
    
    println(a.to_string())
    println(b.to_string())
    println(c.to_string())
}
