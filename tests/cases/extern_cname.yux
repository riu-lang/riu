; 验证 #CName 字符串语法：yux 调用名 strLen 映射到 C ABI 符号 lstrlenA
extern {
  #CName("lstrlenA")
  fn strLen(lpString Ptr) i32
}

fn main() {
  ; "hi" + NUL = 长度 2
  #Mut let a Array<u8> = [104u8, 105u8, 0u8]
  println(strLen(a))
}
