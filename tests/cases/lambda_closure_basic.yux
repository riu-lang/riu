; Phase 4a §6.1 / §6.2：lambda 捕获外层标量 local（值复制语义）

fn main() {
  #Mut let x i32 = 10
  let f = (y i32) i32 => y + x
  println(f(1))   ; 11
  println(f(7))   ; 17

  ; 修改 x 后调用：captures 是值复制 → 闭包不感知后续 x 的变化
  x = 100
  println(f(1))   ; 仍 11（捕获时 x=10）

  ret;
}
