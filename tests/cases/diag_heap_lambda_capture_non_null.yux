; DRAFT-heap-types §5.5 / Phase 3e: Heap<T> 非空按值捕获禁, E4024
; 引导用户改写为 Heap<T>?（B 档 move 捕获）或 Heap<T>&（借用捕获）

fn main() {
  let h Heap<i32> = Heap:<i32>(5i32)
  let f fn()i32 = {
    1i32
  }
  let g fn()i32 = {
    as_ref(h)
  }
}
