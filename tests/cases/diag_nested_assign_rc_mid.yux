; 中段是 Rc<T>（含 RC）时拒收并附 hint。
struct Inner {
  count i32
}

Inner {
  fn Inner(c i32) {
    $.count = c
  }
}

struct Outer {
  state Rc<Inner>
}

Outer {
  fn Outer(s Rc<Inner>) {
    $.state = s
  }
}

fn main() {
  var b Rc<Inner> = Inner(10i32)
  var o Outer = Outer(b)
  o.state.count = 5i32
}
