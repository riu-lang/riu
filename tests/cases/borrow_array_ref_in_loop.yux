; v0.4 Phase 5：循环内借用按块作用域：每次迭代借用消亡

fn main() {
  var a Array<i32> = [10, 20, 30]
  var i i32 = 0
  loop {
    val r Array<i32>& = &a
    println(r.len().to_string())
    i = i + 1
    if i >= 2 {
      break;
    }
  }
  a.push(40)
  println(a.len().to_string())
  ret;
}
