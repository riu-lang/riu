; P1-5 §4.2(1): #Const 方法体内写 $.f → E3110

struct Cell {
    v i32

    #Static
    fn make(v i32) Cell {
        ret Self {
            .v = v
        }
    }
    #Const
    fn bump() {
        $.v = $.v + 1
        ret;
    }
}

fn main() {
    #Mut let c = Cell::make(0)
    c.bump()
    ret;
}
