; v0.4 Phase 5：Array<T>& 借用消亡后允许 push（块作用域 §8.6.5.8 + Phase 3 修复）

fn main() {
  var a Array<i32> = [1, 2, 3]
  loop {
    val r Array<i32>& = &a
    println(r.len().to_string())
    break;
  }
  a.push(4)
  println(a.len().to_string())
  ret;
}
