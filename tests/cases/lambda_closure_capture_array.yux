; Phase 4a-2 §6.2：lambda 捕获 Array<T>（堆句柄）

fn first(a Array<i32>) i32 {
  ret a[0]
}

fn main() {
  #Mut let arr Array<i32> = [10, 20, 30]
  let f = () i32 => first(arr)
  println(f())  ; 10
  println(f())  ; 10（多次调用共享 captures）
  ret;
}
