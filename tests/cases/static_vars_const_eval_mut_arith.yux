; DRAFT-static-vars Phase 3: #Mut 全局 const-eval 优先
; sema 期求值 → ConstantInitializer（但仍 isConstant=false）

#Mut
let COUNTER i32 = 100 + 50

fn main() {
  println(COUNTER)
  COUNTER = COUNTER + 1
  println(COUNTER)
}
