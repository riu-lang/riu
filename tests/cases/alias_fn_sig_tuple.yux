; spec §3.9.1.2 / §3.9.3.1: 透明类型别名在 fn 形参 / 返回类型位置与目标类型等价
; ret 元组字面量到声明为 alias 的返回类型，过去会误报 E3020
IPair = (i32, i32)

fn swap(p IPair) IPair {
  ret (p.1, p.0)
}

fn main() {
  val pr IPair = (10, 20)
  val sw IPair = swap(pr)
  println(sw.0.to_string())
  println(sw.1.to_string())
  ret;
}
