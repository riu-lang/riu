; Phase 4e §6.5：lambda 体 ret 表达式根 ∈ lambda 自身 T& 形参 → 合法
; lambda `(r i32&) i32& => r`：返回根 = r（lambda 自己的 T& 形参），单源约束满足

fn use_lambda(f fn(i32&)i32&) {
  ; 不调用 f；本测试关注 borrow_checker 接受这个形态
}

fn main() {
  use_lambda((r i32&) i32& => r)
  println("ok")
  ret;
}
