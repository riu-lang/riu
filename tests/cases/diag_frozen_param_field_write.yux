struct Point {
    x i32
    y i32
}

Point {
    fn Point(x i32, y i32) {
        $.x = x
        $.y = y
    }
}

fn shift(#Frozen p Point) {
    p.x = 99
    ret;
}

fn main() {
    let q = Point(1, 2)
    shift(q)
    ret;
}
