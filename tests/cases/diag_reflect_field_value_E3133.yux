; DRAFT-spec-reflect §6: E3133 — Field.value on non-compile-time-known Field reference
; 通过形参传入的 Field& 无法在编译期确定为具体字段

struct Point {
  x i32
  y i32

  fn get_field(f Field&) i32 {
    ; f 是形参，编译期无法确定具体字段 → E3133
    f.value
  }
}

fn main() {
  println("ok")
}
