; DRAFT-static-vars Phase 3: 全局 val struct 字面量 const-eval
; struct 字面量通过 ConstEvaluator 求值 → ConstantInitializer

struct Point {
  x i32
  y i32
}

let ORIGIN Point = Point {
  .x = 0
  .y = 0
}

fn main() {
  println(ORIGIN.x)
  println(ORIGIN.y)
}
