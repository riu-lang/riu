; P2-7 回归：Rc<E> 直接做 match scrutinee（借用语义）

enum Color {
  Red
  Green
  Blue
}

fn main() {
  #Mut let b Rc<Color> = Color::Green
  let s String = match b {
    Color::Red   => "r"
    Color::Green => "g"
    Color::Blue  => "b"
  }
  println(s)
}
