; echo：跳过 argv[0]，其余以空格拼接后换行

fn main() {
  let a Array<String> = args()
  #Mut let first bool = true
  #Mut let i usize = 0
  for s in a {
    if i == 0 {
      i += 1
      continue;
    }
    if !first {
      print(" ")
    }
    first = false
    print(s)
    i += 1
  }
  println("")
}
