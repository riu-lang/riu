; cross-module struct with constructor and method

struct Point {
    x i32
    y i32
}

Point {
    fn Point(x i32, y i32) {
        self.x = x
        self.y = y
    }

    fn sum() i32 = self.x + self.y
}
