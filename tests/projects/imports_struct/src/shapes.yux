; cross-module struct with constructor and method

struct Point {
    x i32
    y i32
}

Point {
    #Static
    fn make(x i32, y i32) Point {
        ret Self {
            .x = x
            .y = y
        }
    }

    fn sum() i32 = $.x + $.y
}
