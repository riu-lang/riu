; 本文件 struct Map 盖住裸名；SDK Map 走路径（含方法实例化）

struct Map {
  n i32
}

fn take_stem(m map.Map<i32, i32>) {
}

fn take_fq(m riu.core.map.Map<i32, i32>) {
}

fn main() {
  let mine = Map {
    .n = 1
  }
  println(mine.n)
  let m = map.Map:<i32, i32>::make()
  if m.is_empty() {
    println(1)
  }
}
