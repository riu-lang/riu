; 泛型 impl 体：实例化后字段 ret 类型不匹配 → E3014（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn as_i32() i32 {
    $.val  ; check: E3014
  }
}

fn main() {
  let s Slot<bool> = Slot:<bool>::make(true)
  s.as_i32()
}
