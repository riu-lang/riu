; Phase 2c: Type::name 调用未标 #Static 的实例方法应报 E3120

struct P {
  x i32

  fn P(x i32) {
    $.x = x
  }

  fn get_x() i32 = $.x
}

fn main() i32 {
  let v i32 = P::get_x()  ; check: E3120
  ret 0i32
}
