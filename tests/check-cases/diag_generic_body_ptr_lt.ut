; require-sdk
; 泛型 fn 体：T 实例化为 Ptr 后 a < b → E3073（Phase C）
fn lt_t<T>(a T, b T) {
  a < b  ; check: E3073
  ret;
}

fn main() {
  #Mut let a Ptr = ptr_from_addr(1u64)
  #Mut let b Ptr = ptr_from_addr(2u64)
  lt_t:<Ptr>(a, b)
  ret;
}
