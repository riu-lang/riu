; Phase 4c §6.3：含 T& 捕获的 lambda 不可作 ret 表达式（不可逃逸出借用源 scope）

fn make() fn(i32)i32 {
  #Mut let n i32 = 1
  let r i32& = &n
  ret (x i32) i32 => r + x  ; check: E4022
}

fn main() {
  #Mut let f fn(i32)i32 = make()
  println(f(5))
  ret;
}
