; 同 arity 重载约定 Function 形参反推 lambda 后，体类型错由 SemaPass 报（Phase C）

fn apply(f Function<i32, i32>, x i32) i32 {
  ret f(x)
}

fn apply(f Function<i32, i32>, x str) i32 {
  ret f(0)
}

fn main() {
  apply((x) i32 => x + true, 1)  ; check: E3001
}
