; 泛型 impl 体：T 实例化为 i32 后 $.val.foo → E3041（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn poke() {
    $.val.foo = 1  ; check: E3041
    ret;
  }
}

fn main() {
  #Mut let s Slot<i32> = Slot:<i32>::make(1)
  s.poke()
  ret;
}
