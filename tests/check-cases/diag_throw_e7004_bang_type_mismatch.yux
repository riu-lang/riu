; `!` 透传时 caller / callee 错误类型不一致 → E7004（DRAFT-错误.md [#4.B]）
enum ParseErr {
  Empty
}

enum AppErr {
  Bad
}

fn parse() i32 ! ParseErr {
  ret 0i32
}

fn run() i32 ! AppErr {
  #Mut let x i32 = parse()!  ; check: E7004
  ret x
}

fn main() {
  ret;
}
