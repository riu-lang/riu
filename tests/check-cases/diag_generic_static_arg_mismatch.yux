; 泛型 struct #Static fn + turbofish 实参类型不匹配 → E3131（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(true)  ; check: E3131
  ret;
}
