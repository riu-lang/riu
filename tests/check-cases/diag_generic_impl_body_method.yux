; 泛型 impl 体：T 实例化为 i32 后 $.val.len() 不存在 → E3095（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn len() usize {
    $.val.len()  ; check: E3095
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(1)
  s.len()
}
