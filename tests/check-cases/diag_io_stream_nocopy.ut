; require-sdk-modules
; 文件流拥有 OS 句柄，已有输入流不可隐式复制。
; FileInputStream 字段是 HANDLE 别名；.ud 骨架不带 types 通配，调用方需能展开别名才能算布局。
use riu.io.*
use riu.platform.windows.types.*

fn copy_input(stream FileInputStream) {
  let copied FileInputStream = stream  ; check: E4031  #NoCopy 文件输入流不可隐式复制
}
