; 泛型 impl 体：T 实例化为 i32 后 $.val[0] → E3062（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn peek() {
    $.val[0]  ; check: E3062
    ret;
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(1)
  s.peek()
  ret;
}
