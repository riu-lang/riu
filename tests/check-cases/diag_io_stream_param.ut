; require-sdk-modules
; 文件流拥有 OS 句柄，已有输出流不可按值传参。

use riu.io.*

fn consume_output(stream FileOutputStream) {
}

fn pass_output(stream FileOutputStream) {
  consume_output(stream)  ; check: E4031  #NoCopy 文件输出流不可按值传参
}
