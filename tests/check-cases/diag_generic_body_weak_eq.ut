; 泛型 fn 体：T 实例化为 Weak<i32> 后 a == b → E3078（Phase C）
fn eq_t<T>(a T, b T) {
  a == b  ; check: E3078
  ret;
}

fn main() {
  #Mut let b Rc<i32> = 10i32
  #Mut let w Weak<i32> = b
  eq_t:<Weak<i32>>(w, w)
  ret;
}
