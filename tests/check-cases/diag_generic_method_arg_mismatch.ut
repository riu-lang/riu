; 泛型 struct 实例方法：接收者 typeArgs 替换后实参不匹配 → E3014（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn set_val(v T) {
    $.val = v
  }
}

fn main() {
  #Mut let s Slot<i32> = Slot:<i32>::make(1)
  s.set_val(true)  ; check: E3014
  ret;
}
