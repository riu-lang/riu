; `!` 出现在非 T ! E 函数内 → E7001（DRAFT-错误.md [#4.B]）
enum ParseErr {
  Empty
}

; [#3.F]：允许声明 T ! E 但 body 不实际抛错（接口先行）
fn parse() i32 ! ParseErr {
  ret 0i32
}

fn main() {
  #Mut let x i32 = parse()!  ; check: E7001
  ret;
}
