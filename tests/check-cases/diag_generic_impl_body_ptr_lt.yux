; require-sdk
; 泛型 impl 体：写出 Slot<Ptr> 后 $.val < other → E3073（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn lt_val(other T) {
    $.val < other  ; check: E3073
    ret;
  }
}

fn main() {
  let s Slot<Ptr> = Slot:<Ptr>::make(GetStdHandle(-11i32))
  s.lt_val(GetStdHandle(-12i32))
  ret;
}
