; 静态 fallible 调用以 `!` 透传时，caller / callee 错误类型不一致 → E7004

enum ParseErr {
  Invalid
}

enum IoErr {
  Failed
}

struct Parser {
  #Static
  fn parse() i32 ! ParseErr {
    ret 1
  }
}

fn caller() i32 ! IoErr {
  ret Parser::parse()!  ; check: E7004
}
