; §5.1.2 / const-mut：#Frozen 局部禁字段写 → E3106
struct Point {
  x i32
  y i32

  #Static
  fn make(x i32, y i32) Point {
    ret Self {
      .x = x
      .y = y
    }
  }
}

fn main() {
  #Frozen let p Point = Point::make(1, 2)
  p.x = 99  ; check: E3106
  ret;
}
