; DRAFT-spec-default-body Phase 2：spec 默认体内调用不在签名集内的方法
; 应被 sema 占位符号校验 (E1140) 命中。lt 调用了 nonexistent，spec 内没有声明。

#Spec
struct Ord {
  fn cmp(other Self&) i32
  fn lt(other Self&) bool = $.nonexistent(other) < 0  ; check: E1140
}

fn main() {
  println("ok")
}
