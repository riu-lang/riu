; 泛型 impl 体：实例化后字段赋值类型不匹配 → E3014（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn set_i32(v i32) {
    $.val = v  ; check: E3014
  }
}

fn main() {
  #Mut let s Slot<bool> = Slot:<bool>::make(true)
  s.set_i32(1)
}
