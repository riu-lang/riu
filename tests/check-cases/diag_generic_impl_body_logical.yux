; 泛型 impl 体：写出 Slot<i32> 后 $.val && true → E3001（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn and_true() {
    $.val && true  ; check: E3001
    ret;
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(1)
  s.and_true()
  ret;
}
