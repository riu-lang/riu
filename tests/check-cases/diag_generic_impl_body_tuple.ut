; 泛型 impl 体：写出 Slot<i32> 后 $.val 解构不是元组 → E3101（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn unpack() {
    let (a, b) = $.val  ; check: E3101
    ret;
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(1)
  s.unpack()
  ret;
}
