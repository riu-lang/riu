; 泛型 impl 体：T 实例化为 bool 后 $.val + 1 → E3001（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn add1() {
    $.val + 1  ; check: E3001
  }
}

fn main() {
  let s Slot<bool> = Slot:<bool>::make(true)
  s.add1()
}
