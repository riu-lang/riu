; DRAFT-spec-reflect Phase 1: spec body 内 instance 字段段仍拒 (E2011).
; 仅 `#Static` 字段段被放行 ([#1.Q] 例外 / [#1.Z]).

#Spec
struct Point {
  x i32  ; check: E2011
  fn norm() i32
}

fn main() {
  println(0)
}
