; try 块内调用 IoErr ! E 函数，但 catch 子句只声明 ParseErr → E7002
enum IoErr {
  NotFound
}

enum ParseErr {
  Empty
}

fn read() i32 ! IoErr {
  ret 0i32
}

fn main() {
  #Mut let x i32 = try {  ; check: E7002
    read()
  } catch e ParseErr {
    ret;
  }
  ret;
}
