; 泛型 impl 体：T 实例化为 i32 后 $.val?.foo → E3024（Phase C）
struct Slot<T> {
  val T

  #Static
  fn make(v T) Self {
    Self {
      .val = v
    }
  }

  fn peek() {
    $.val?.foo  ; check: E3024
    ret;
  }
}

fn main() {
  let s Slot<i32> = Slot:<i32>::make(1)
  s.peek()
  ret;
}
