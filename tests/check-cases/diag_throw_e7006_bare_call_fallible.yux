; 调用 T ! E 函数但未加 `!` → E7006（DRAFT-错误.md [#4.B]）
enum ParseErr {
  Empty
}

fn parse() i32 ! ParseErr {
  ret 0i32
}

fn run() i32 ! ParseErr {
  #Mut let x i32 = parse()  ; check: E7006
  ret x
}

fn main() {
  ret;
}
