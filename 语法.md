# Yux 语法

### 注释

```yux
/ 顶行/，其它位置用为除号
fn some() {
  / 也可缩进
  println(123) ; 空格;为尾随注释
}
```

### 类型系统

#### 基本类型

| 类型 | 说明 | LLVM类型 |
|------|------|----------|
| `bool` | 布尔值 | i1 |
| `i8` | 8位有符号整数 | i8 |
| `u8` | 8位无符号整数 | i8 |
| `i16` | 16位有符号整数 | i16 |
| `u16` | 16位无符号整数 | i16 |
| `i32` | 32位有符号整数 | i32 |
| `u32` | 32位无符号整数 | i32 |
| `i64` | 64位有符号整数 | i64 |
| `u64` | 64位无符号整数 | i64 |
| `f32` | 32位浮点数 | float |
| `f64` | 64位浮点数 | double |

#### 导入

```yux
use pkg1.pkg2.some
use pkg1.*
```

### 项目（暂定）

项目根由 `yux.toml` 标记。命令行两种模式：

- 单文件模式：`yux <src.yux>` —— 忽略 yux.toml，产物扁平放在 `<源文件目录>/build/`
- 项目模式：`yux build <name>` —— 必须在 yux.toml 所在目录执行，`<name>` 须与 toml 的 `name` 匹配

`yux.toml` 最小字段：

```toml
name = "test"       ; 项目 / 可执行文件名
entry = "main.yux"  ; 入口源文件，相对项目根
version = "1.0.0"   ; 版本号（当前仅记录，后续可能参与产物命名 / 依赖解析）
```

模块解析：`use a.b.c` 映射到 `<projectRoot>/a/b/c.yux`（文件）或 `<projectRoot>/a/b/c/`（包目录）；同名 `.yux` 与同名目录并存属冲突。

#### 数组类型

数组类型语法：`[类型 * 数量]`

```yux
val arr [i32 * 5] = [1, 2, 3, 4, 5]
val bytes [u8 * 4] = [97u8, 98u8, 99u8, 100u8]
```

#### 泛型类型

泛型类型语法：`类型名<类型参数>`

```yux
/ Ref 是内置的引用类型
val ref Ref<Point> = &point

/ Box 是内置的堆对象类型
val box Box<Point> = Point(1.0, 2.0)

/ 泛型类型作为函数参数
fn modify(p Ref<Point>) {
  p.x = 10.0
}
```

#### 引用类型 Ref<T>

`Ref<T>` 是内置的不可空引用类型，编译为指针：

```yux
struct Point {
  x f64
  y f64
}

Point {
  fn move_to(self Ref<Point>, dx f64, dy f64) {
    self.x = self.x + dx
    self.y = self.y + dy
  }
}

fn main() {
  var p = Point(1.0, 2.0)
  val ref = &p  ; 取引用
  ref.move_to(1.0, 1.0)
  println(p.x)  ; 输出 2.0
}
```

**特性：**
- `Ref<T>` 是不可空引用，编译为原始指针
- 使用 `&变量` 或 `&变量.字段` 获取引用
- 函数参数为 `Ref<T>` 时，可传入 `T` 类型的值，自动取引用
- `Ref<T>` 不支持函数类型

#### 堆对象类型 Box<T>

`Box<T>` 是内置的堆分配智能指针，自动管理内存：

```yux
struct Point {
  x f64
  y f64
}

Point {
  fn Point(x f64, y f64) {
    self.x = x
    self.y = y
  }
}

fn main() {
  var p Box<Point> = Point(1.0, 2.0)
  println(p.x) ; 输出 1.0
  
  var p2 Box<Point> = p ; 复制，引用计数+1
  println(p2.y) ; 输出 2.0
} ; p 和 p2 离开作用域，引用计数归零，自动释放内存
```

**特性：**
- `Box<T>` 是不可空的堆对象指针
- 使用引用计数自动管理内存（类似 `shared_ptr`）
- 复制 `Box<T>` 会增加引用计数
- 离开作用域时自动减少引用计数，归零时释放内存
- 成员访问与普通对象一致（自动解引用）
- 使用 Windows API (`HeapAlloc`/`HeapFree`) 管理内存

**内存布局：**
```
Box<T> 结构:
┌─────────────┬─────────────┐
│ data_ptr    │ refcount_ptr│
│ (指向T数据)  │ (指向引用计数)│
└─────────────┴─────────────┘

堆内存布局:
┌────────────┬─────────────────┐
│ refcount   │ T data          │
│ (i64)      │                 │
└────────────┴─────────────────┘
```

#### 原始指针类型 Ptr<T>

`Ptr<T>` 是内置的原始指针类型，可空：

```yux
/ 声明可空指针
var p Ptr<i32> = null

/ 从 Box<T> 或 Ref<T> 转换
struct Data {
  value i32
}

fn main() {
  var data Data = Data(42)
  val ref = &data
  var ptr Ptr<Data> = ref   ; Ref<T> -> Ptr<T>

  var box Box<Data> = Data(100)
  var ptr2 Ptr<Data> = box  ; Box<T> -> Ptr<T>
}
```

**特性：**
- `Ptr<T>` 是可空的原始指针，用 `null` 表示空指针
- 可以从 `Ref<T>` 或 `Box<T>` 直接转换
- 没有成员函数，不进行析构
- 编译为 LLVM 原始指针

#### 堆动态数组类型 Array<T>

`Array<T>` 是内置的堆分配动态数组类型，支持运行时确定长度：

```yux
fn main() {
  / 创建空数组
  var empty Array<i32> = []
  
  / 创建带初始值的数组
  var arr Array<i32> = [1, 2, 3]
  
  / 索引访问
  println(arr[0]) ; 输出 1
  
  / 索引赋值
  arr[0] = 10
  println(arr[0])  ; 输出 10
  
  / 不同元素类型
  var arr_f64 Array<f64> = [1.5, 2.5, 3.5]
  println(arr_f64[1])
}
```

**特性：**
- `Array<T>` 是堆分配的动态数组
- 使用 Windows API (`HeapAlloc`/`HeapFree`) 管理内存
- 离开作用域时自动释放内存
- 支持空数组初始化 `[]`
- 索引访问和赋值语法与固定数组相同

**内存布局：**
```
Array<T> 结构:
┌─────────────┬─────────────┬─────────────┐
│ data_ptr    │ len         │ cap         │
│ (指向T数据)  │ (当前长度)   │ (容量)      │
└─────────────┴─────────────┴─────────────┘
```

#### 无后缀整数字面量的类型推断

无后缀整数字面量（如 `1`、`42`）的类型由上下文自动推断：

**推断规则：**
- 默认类型为 `i32`
- 在二元运算中，若另一侧为已知整数类型，字面量自动匹配该类型
- 在赋值/声明时，若目标变量有显式整数类型，字面量自动匹配
- 在 `ret` 语句中，若函数有显式整数返回类型，字面量自动匹配
- 在函数调用中，字面量会参与重载解析：
  - 若按默认类型（i32）能唯一匹配某一重载，使用该重载
  - 若默认无匹配但通过自动转换能唯一匹配某一重载，使用该重载并将字面量推断为该参数类型
  - 若多个重载均可匹配，报错（需添加类型后缀消歧）

```yux
/ 二元运算中的自动推断
val a u8 = 1u8 + 2       ; 2 推断为 u8
val b i64 = 100 + 200    ; 100 和 200 推断为 i64
val c u32 = 5u32 * 3 + 1 ; 3 和 1 推断为 u32

/ 声明/赋值时的推断
var x u8 = 42            ; 42 推断为 u8
x = 10                   ; 10 推断为 u8
x += 5                   ; 5 推断为 u8

/ 返回值推断
fn big() i64 {
  ret 123456789          ; 字面量推断为 i64
}

/ 函数调用中的重载解析
fn f(a i32, b u8) { }
val r = f(1, 2)          ; 2 自动推断为 u8（唯一匹配）

/ 不能唯一确定时报错
fn g(a u8, b u8) { }
fn g(a i64, b u8) { }
/ g(1, 2) ; ❌ 错误：多个重载均可匹配
g(1u8, 2u8)              ; ✅ 显式后缀消歧
```

#### 类型转换

**不存在隐式转换，所有类型必须显式转换。**

使用 `.to_类型()` 方法进行类型转换：

```yux
val a i32 = 10
val b f64 = a.to_f64() ; i32 -> f64
val c i64 = a.to_i64() ; i32 -> i64
val d u8 = 100u8
val e i32 = d.to_i32() ; u8 -> i32
```

支持的转换方法：
- `to_i8()`, `to_u8()`, `to_i16()`, `to_u16()`
- `to_i32()`, `to_u32()`, `to_i64()`, `to_u64()`
- `to_f32()`, `to_f64()`

### 函数

```yux
/ 没参数
fn nop() {

}

/ 表达式作为函数体
fn add(a i32, b i32) i32 = a + b

/ 标准写法
fn sub(a i32, b i32) i32 {
  ret a + b ; 也可以直接 a + b 类似rust
}

/ _开头私有函数，结构体也适用
fn _pri_fn() {

}
```

#### 返回语句

函数可以使用 `ret` 关键字返回值：

```yux
/ 返回值
fn add(a i32, b i32) i32 {
  ret a + b
}

/ 空返回（返回 void）
fn early_exit(flag bool) {
  if flag {
    ret; ; 提前返回，必须尾随 ;
  }
  println(123)
}
```

**空返回规则：**
- `ret;` 用于在 void 函数中提前返回
- 必须尾随 `;` 表示空返回
- 常用于条件判断中的提前退出

#### 泛型函数

Yux 支持泛型函数，通过类型参数实现代码复用：

```yux
/ 定义泛型函数
fn <T> identity(x T) T = x

fn <T> add(a T, b T) T = a + b

/ 显式指定类型参数调用
val a i32 = identity<i32>(42)
val b i64 = identity<i64>(100)

/ 类型推断调用（从实参类型推断 T）
val c i32 = identity(999)  ; T 推断为 i32
val d i32 = add(1, 2)      ; T 推断为 i32

/ 多类型参数
fn <K, V> make_pair(k K, v V) Pair<K, V> {
  ret Pair<K, V>(k, v)
}
```

**泛型函数特性：**
- 使用 `fn <T1, T2, ...>` 语法定义类型参数
- 调用时可通过 `<类型>` 显式指定类型参数
- 若未显式指定，编译器会从实参类型推断
- 无后缀整数字面量会根据显式类型参数推断类型
- 泛型函数采用单态化（monomorphization）实现，每个实例独立编译

**类型推断规则：**
- 显式类型参数优先：`identity<i64>(100)` 中 `100` 推断为 `i64`
- 无显式类型参数时，从实参类型推断：`identity(x)` 中 T 为 x 的类型
- 多个参数时，所有使用同一类型参数的参数类型必须一致

### 变量声明

```yux
/ var 可变变量
var a i32 = 10
a = 20

/ val 不可变变量
val b i64 = 100
/ b = 200 ; 错误：不可重新赋值

/ 类型推断
val c = 123  ; 推断为 i32
val d = 3.14 ; 推断为 f64
val e = true ; 推断为 bool
```

### 全局常量

使用 `cval` 关键字声明全局常量，在编译时确定值：

```yux
/ 公开全局常量
cval MAX_SIZE i32 = 1024
cval PI f64 = 3.14159

/ 私有全局常量（_开头）
cval _INTERNAL_CONST i32 = 42

fn main() {
  println(MAX_SIZE)
  println(_INTERNAL_CONST)
}
```

**特性：**
- 全局常量在编译时确定值，存储在可执行文件的数据段
- `_` 开头的全局常量为私有，仅在当前模块可见
- 目前支持基本类型：`i8`, `i16`, `i32`, `i64`, `u8`, `u16`, `u32`, `u64`, `f32`, `f64`, `bool`
- 全局常量不可修改，在整个程序运行期间保持不变

### 字面量

#### 整数字面量

```yux
val a = 42     ; 十进制
val b = 0b1010 ; 二进制
val c = 0o755  ; 八进制
val d = 0xFF   ; 十六进制

/ 带类型后缀
val e = 42i8
val f = 100u64
val i = -123i32
```

支持的后缀：
- 有符号：`i8`, `i16`, `i32`, `i64`
- 无符号：`u8`, `u16`, `u32`, `u64`

无后缀整数字面量的类型会根据上下文自动推断（默认 `i32`），详见上方"无后缀整数字面量的类型推断"一节。

#### 浮点字面量

```yux
val a = 3.14
val b = 2.5f32 ; 32位浮点
val c = 1.0f64 ; 64位浮点
val d = -0.5
```

#### 布尔字面量

```yux
val a = true
val b = false
```

#### 数组字面量

```yux
val arr = [1, 2, 3] ; 默认 i32 数组
val bytes = [97u8, 98u8] ; u8 数组
```

#### 字符串字面量

```yux
/ 双引号字符串，支持转义
val s1 = "hello world"
val s2 = "line1\nline2"     ; \n 换行
val s3 = "tab\there"        ; \t 制表符
val s4 = "quote: \"text\""  ; \" 双引号

/ 原始字符串（r前缀），不处理转义
val path = r"C:\path\to\file"  ; 反斜杠无需转义
val regex = r"\d+\.\d+"        ; 正则表达式
```

**转义字符：**
- `\n` - 换行
- `\t` - 制表符
- `\\` - 反斜杠
- `\"` - 双引号
- `\'` - 单引号

**原始字符串：**
- 以 `r"` 开头，`"` 结尾
- 不处理任何转义字符，内容按字面意义保留
- 不能包含 `"`（无转义机制）和换行符
- 适用于文件路径、正则表达式等场景

#### 代码点字面量

使用 `c'X'` 语法表示单个 Unicode 代码点，类型为 `u32`：

```yux
val a u32 = c'A'    ; 65
val b u32 = c'\n'   ; 10
val c u32 = c'中'   ; 20013
```

**支持的转义：** `\n` `\r` `\t` `\v` `\b` `\0` `\\` `\'`

**限制：**
- 引号内仅允许单个代码点（一个字符或一个转义序列）
- 不支持 `\xNN` / `\uNNNN` 数值转义，也没必要

#### 数组填充

使用 `[值 ...]` 语法创建填充数组，所有元素初始化为相同值：

```yux
/ 基本用法：必须指定数组类型和长度
var buf [u8 * 21] = [0u8 ...] ; 创建长度为21的u8数组，全部初始化为0

/ 显式指定元素类型
var arr [i32 * 10] = [0 ... i32] ; 创建长度为10的i32数组，全部初始化为0

/ 使用非零值填充
var flags [bool * 5] = [true ...] ; 创建长度为5的bool数组，全部初始化为true
```

**重要规则：**

1. **必须指定类型和长度**：数组填充表达式必须配合类型注解使用
   ```yux
   val arr = [0 ...] ; ❌ 错误：缺少类型注解
   val arr [i32 * 5] = [0 ...] ; ✅ 正确
   ```

2. **类型必须匹配**：字面量类型必须与目标元素类型一致
   ```yux
   val arr [i32 * 5] = [0u8 ...] ; ❌ 错误：u8 与 i32 不匹配
   val arr [i32 * 5] = [0 ...] ; ✅ 正确：推断为 i32
   val arr [i32 * 5] = [0i32 ...] ; ✅ 正确：显式指定 i32
   ```

3. **显式类型必须与字面量匹配**：
   ```yux
   val arr [i32 * 5] = [0u8 ... i32] ; ❌ 错误：字面量是 u8，显式类型是 i32
   val arr [i32 * 5] = [0 ... i32] ; ✅ 正确
   ```

### 表达式

#### 算术运算

```yux
val a = 10 + 5 ; 加法
val b = 10 - 5 ; 减法
val c = 10 * 5 ; 乘法
val d = 10 / 5 ; 除法
val e = 10 % 3 ; 取模
```

#### 复合赋值运算

复合赋值运算符将运算和赋值合并为一步：

```yux
var a i32 = 10
a += 5   ; 等价于 a = a + 5，结果为 15
a -= 3   ; 等价于 a = a - 3，结果为 12
a *= 2   ; 等价于 a = a * 2，结果为 24
a /= 4   ; 等价于 a = a / 4，结果为 6
a %= 4   ; 等价于 a = a % 4，结果为 2

/ 位运算复合赋值
var b i32 = 16
b >>= 2  ; 等价于 b = b >> 2，结果为 4（右移）
b <<= 1  ; 等价于 b = b << 1，结果为 8（左移）
```

**支持的复合赋值运算符：**
- `+=` 加法赋值
- `-=` 减法赋值
- `*=` 乘法赋值
- `/=` 除法赋值
- `%=` 取模赋值
- `>>=` 右移赋值
- `<<=` 左移赋值

**注意：** 复合赋值运算符两边必须有空格。

#### 比较运算

```yux
val a = 10 == 5 ; 等于
val b = 10 != 5 ; 不等于
val c = 10 > 5  ; 大于
val d = 10 >= 5 ; 大于等于
val e = 10 < 5  ; 小于
val f = 10 <= 5 ; 小于等于
```

#### 一元运算

```yux
val a = -10     ; 取负（整数）
val b = -3.14   ; 取负（浮点数）
val c = ~5      ; 按位取反（整数）
val d = !true   ; 逻辑非（布尔值）
```

**运算符说明：**
- `-` 取负：适用于整数和浮点数类型
- `~` 按位取反：仅适用于整数类型
- `!` 逻辑非：仅适用于布尔类型

**注意：** 一元运算符与操作数之间不能有空格。

#### 运算符优先级（从低到高）

1. 比较运算符：`==`, `!=`, `>`, `>=`, `<`, `<=`
2. 加减运算符：`+`, `-`
3. 乘除模运算符：`*`, `/`, `%`
4. 一元运算符：`!`, `~`, `-`（无空格）
5. 后缀运算符：函数调用 `.`, `[]`, `()`

使用括号改变优先级：

```yux
val a = (1 + 2) * 3
```

#### 数组访问

```yux
val arr = [10, 20, 30]
val first = arr[0]
arr[1] = 25
```

#### 成员访问

```yux
val n = N(1.5)
val x = n.field
```

#### 函数调用

```yux
val result = add(1, 2)
println(result)
```

### 控制流

#### if/elif/else

`if` 可以作为表达式使用，也可以作为语句使用：

```yux
/ 作为表达式
val max = if a > b {
  a
} else {
  b
}

/ 带有 elif
val grade = if score >= 90 {
  1
} elif score >= 80 {
  2
} elif score >= 70 {
  3
} else {
  4
}

/ 作为语句（尾随;表示空返回）
if x > 0 {
  println(x);
}
```

#### 单行 if-else 表达式

支持单行 if-else 表达式，适用于简单的条件选择：

```yux
/ 单行 if-else 表达式
val max = if a > b { a } else { b }
val sign = if x >= 0 { 1 } else { -1 }
```

**语法规则：**
- `if 条件 { 真值 } else { 假值 }`
- 条件、真值、假值两边必须有空格
- 真值和假值类型必须一致

#### Python 风格三元表达式

支持类似 Python 的三元表达式语法：

```yux
/ Python 风格三元表达式
val max = a if a > b else b
val sign = 1 if x >= 0 else -1
val msg = "positive" if x > 0 else "non-positive"
```

**语法规则：**
- `真值 if 条件 else 假值`
- `if` 和 `else` 两边必须有空格
- 真值和假值类型必须一致

#### loop 循环

```yux
var i i32 = 0
loop {
  if i >= 10 {
    break;
  }
  println(i)
  i = i + 1
}
```

`break` 必须尾随 `;` 表示空返回。

### 结构体

#### 声明结构体

```yux
struct Point {
  x f64
  y f64
}

struct Person {
  name [u8 * 32]
  age i32
}
```

#### 实现结构体

```yux
Point {
  / 析构函数（必须在第一位）
  fn ~() {
    / 清理资源
  }

  / 构造函数（与结构体同名）
  fn Point(x f64, y f64) {
    self.x = x
    self.y = y
  }

  / 成员函数
  fn add(other Point) Point {
    Point(self.x + other.x, self.y + other.y)
  }
}
```

#### 析构/清理函数

析构函数使用 `fn ~()` 语法定义，在结构体实现块中必须位于第一位。

**特性：**
- 析构函数无参数、无返回值
- 当结构体变量离开作用域时自动调用
- 如果未定义析构函数，则不会自动生成

```yux
struct Resource {
  data i32
}

Resource {
  fn ~() {
    / 离开作用域时自动调用
    self.data = 0
  }

  fn Resource(val i32) {
    self.data = val
  }
}

fn test() {
  var r Resource = Resource(42)
  println(r.data) ; 输出 42
} ; r 离开作用域，析构函数自动调用
```

#### 使用结构体

```yux
val p1 = Point(1.0, 2.0)
val p2 = Point(3.0, 4.0)
val p3 = p1.add(p2)
```

### 空格规则

- 关键字后必须有空格
- 二元运算符两边必须有空格
- `()` `[]` 内部无空格
- `,` 后有空格

```yux
/ 正确
fn add(a i32, b i32) i32 = a + b
val arr = [1, 2, 3]

/ 错误
fn add(a i32,b i32)i32=a+b ; 缺少空格
val arr = [ 1,2,3 ]        ; 括号内多余空格
```

### 编译流程

1. **词法分析**：使用 ANTLR4 生成的词法分析器将源代码转换为 Token 流
2. **语法分析**：使用 ANTLR4 生成的语法分析器构建 AST
3. **语义分析**：ASTBuilder 遍历 AST，构建符号表和类型信息
4. **IR 生成**：Compiler 将 AST 转换为 LLVM IR
5. **链接**：使用 LLD 链接器生成可执行文件

#### 编译命令

```shell
#编译 输出到工作目录build下
yux input.yux
yux --emit-ir input.yux 
# 仅debug版本有该参数
yux -d input.yux
```

#### 输出

- 编译成功后在工作目录输出到 `build` 目录
- `--emit-ir` 会在 `build` 目录生成 `.ll` 文件

### SDK

Yux 提供了自举的运行时库（SDK），包含：

- `print()` / `println()` - 打印函数，支持所有基本类型
- `Ref<T>` - 内置不可空引用类型
- `Box<T>` - 内置堆对象智能指针类型
- `Ptr<T>` - 内置原始指针类型，可空

SDK 位于 `sdk/sdk.yux`，编译时自动链接。

### 外部声明

使用 `extern` 关键字声明外部函数，可调用 C/C++ 或系统 API：

```yux
/ 声明外部 Windows API
extern {
  fn GetStdHandle(nStdHandle i32) Ptr<u8>
  fn WriteFile(hFile Ptr<u8>, lpBuffer Ptr<u8>, nNumberOfBytesToWrite u32, lpNumberOfBytesWritten Ptr<u32>, lpOverlapped Ptr<u8>) i32
}
```

**特性：**
- `extern` 块中声明的函数不包含函数体
- 声明的函数会在链接时从外部库解析
- 可调用任何 ABI 兼容的外部函数（如 C 库、Windows API 等）
- 结合 `Ptr<T>` 可与外部 API 交互
