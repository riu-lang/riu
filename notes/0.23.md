# v0.23.0-alpha

进行中。目标约 2026-09-27。切版：三套测试全绿、`./lint` 0 warnings。

## 2026-09-13

- **加**：`kBuiltinMethods` 从 `sema/` 迁到 `riu/include/`（编译器分离 1.1）；`file_node.cpp` 不再 include `sema/`
- **修**：E3030 拼写建议改在 Sema 重抛时附加（1.2）；`expr_node.cpp` / `literal_node.cpp` 不再 include `analyzer/`
- **加**：`resolveTypePath` 下沉到 `riu/ast/type_path.{h,cpp}`（1.3）；`type_node.cpp` 不再 include `sema/`；NameResolver 包一层给 Sema
- **修**：全局 `#Cval` / `#Mut` / val init 的 const-eval 从 builder 改到 SemaPass（1.4 选 b）；`ast_builder_decl.cpp` 不再 include `sema/`；E3140 随 Sema 抛
- **修**：match arm 绑定类型从 builder 改到 SemaPass（1.5）；`ast_builder_expr.cpp` 不再 include `sema/`
- **修**：`SpecRegistry` / `validateSpecImpls` 与 `Riu` 构造/析构从 `riu.cpp` 挪到 `analyzer/riu_spec.cpp`（1.6）；`riu.cpp` 不再 include `analyzer/`；`SemaPass::run` 起始处兜底校验
- **修**：结构叶 `getType()` 优先读 resolved 槽（1.7a）；Sema 先下钻再经 `type_of_struct` 写槽；泛型 codegen 用 `structuralType()` 无槽重算

## 2026-09-14

- **修**：Get / GetRef `getType()` 优先读 resolved 槽（1.7b）；Sema 经 `type_of_name` 写槽（Get 先下钻索引基）；GetRef 作用域查找仍在 `structuralType` 供无槽重算。codegen 不再给这两类节点填槽，避免 spec 默认体复用 AST 时锁死上次 impl 的类型
- **修**：Unary / AddSub / MulDiv / BinOp / Compare `getType()` 优先读 resolved 槽（1.7c）；Sema 先下钻再经 `type_of_ops` 写槽；运算符方法仍走 `tryValidate*`（call_resolve）。codegen 不再给这几类节点填槽
- **修**：Call / PathCall `getType()` 优先读 resolved 槽（1.7d）；Sema 先下钻再经 `type_of_call` 写槽；重载 / 内建方法 / #Static fn 仍走 Sema 分支（call_resolve / NameResolver）。codegen 不再填槽；subst 帧 `resolvedOrInferredType` 始终 `structuralType`+替换，避免复用模板 AST 时锁死上一实例
- **修**：Dot `getType()` 优先读 resolved 槽（1.7e）；Sema 先下钻基表达式再经 `type_of_dot` 写槽；字段 / 方法 / `?.` / Field.value 仍在 `structuralType`。方法点 E3095 仍由 Call 的 writeResolved 记下。灵活整数回填后 Dot 槽经 `structuralType` 重算。codegen 不再填槽
- **修**：IfElse / OneLineIfElse / Match / TryCatch / NullElse `getType()` 优先读 resolved 槽（1.7f）；Sema 先下钻再经 `type_of_ctrl` 写槽；汇合 / 流终止跳过 / `??` 剥 Nullable 仍在 `structuralType`。if/match/try 空 `[]` 靶向仍在 finishCtrlResolved。codegen 不再填槽
- **修**：Lambda / StructLit / DynCtor / MoveAssign `getType()` 优先读 resolved 槽（1.7g）；Sema 先下钻再经 `type_of_rest` 写槽；Fn 字面量 / Self·TypeName 字面量 / Dyn 包装 / `<-` 左侧仍在 `structuralType`。codegen 不再填槽
- **修**：SDK 自构建只对 obj 过期的模块整文件 parse，其余读 `.ud`；ANTLR 先 SLL、失败再 LL。debug：全量 SDK 编 ~19s→~14.5s，改一个文件不再拖 12 个一起 parse；增量 `riu test` ~6s

## 2026-09-15

- **修**：`NameResolver` / `resolveAlias` / `resolveExprTypeLhs` 下沉到 `riu/ast/name_lookup.{h,cpp}`；`DiagnosticEngine` / `SyntaxErrorListener` 下沉到 `riu/ast/`（parse 渲染，1.8）；`expr_node.cpp` 不再 include `sema/`；`riu_ast` 去掉 `include_dirs = [ "//riu/frontend" ]`；`rules/sema-codegen.md` / `riu.md` 写 include 禁令
- **修**：`Ensure-SdkLink` 核对 junction 目标；已存在但指向旧路径（如 `D:\yux-lang\sdk`）时拆掉重建，不再 `Test-Path` 就跳过
- **加**：`examples/grep` / `wc` / `freq` / `tree`（搜行、计数、词频、目录树）；删 `examples/test` 语法展览
- **修**：`examples/README.md`、`docs/第一个程序.md` 清单跟上
- **修**：if/else（及 match 无值臂、一行 if）尾表达式即使结果不需析构也开临时子帧。原先 void `println("${n}")` 的 String 临时记到外层帧、在 merge 上释放未走那支的 spill → SIGSEGV。回归：`tests/projects/tpl_if_println`、`print_generic.test.ut` `test_println_template_in_if`
- **加**：语言改名为 riu（发音通 U）；源 `.ut` / 声明 `.ud`；`riu.toml`；SDK `riu.core`；CLI / 运行时 / LLVM 符号一并改
- **加**：抽出 `riu_generic` 空壳（2.1）；`StructInstance` / `FnInstance` / `SubstFrame` 占位；`riu_frontend` / `riu` 加 dep；行为零变化
- **修**：`applySubst` / `_substStack` / `bindStructSelfType` 迁入 `riu_generic`（2.2）；codegen 经 generic API 套替换；Sema `applySubstMap` / `applyInstSubst` 走同一 `generic::applySubstMap`
- **修**：`ensureStructInstance` + `_structInstances` 迁入 `riu_generic`（2.3）；登记 / 查询在 generic，LLVM 类型与 `emitInstanceMethods` 仍在 Compiler
- **修**：`ensureFnInstance` / `ensureMethodInstance` + `_fnInstances` 迁入 `riu_generic`（2.4）；登记 / 查询在 generic，IR 仍 `emitFnInstances`

## 2026-09-16

- **修**：SemaPass 登记 generic 实例表、用 `SubstStack` 再走模板体（2.5）；删 `_instSubst` / `_checkedGenericInst`。栈用 deque，嵌套实例化压帧不使 Sema 持有的 subst 指针失效
- **修**：codegen 实例表改挂 `generic::Registry`（2.6）；`ensure*Instance` 的登记 / key 在 generic，Compiler 只问已具体实例再发 LLVM 类型与 IR
- **修**：删 `compileFn` / `compileMethodImpl` 里 borrow / const-mut / `#NoReturn` 三处 checker（3.1）；只走 `SemaPass::visitFn`，codegen 不再双跑
- **修**：三个 checker 从 `SemaPass::visitFn` 抽出，Sema 之后由 driver 调 `runFnCheckers`（3.2）；`compiler.cpp` 无 `analyzer/` include；`riu-check` 同一点。诊断顺序改为 Sema 先于 checker（未定义名 E3030 不再被 E4002 挡住）
- **加**：`AstVisitor` 对每个表达式 / 语句节点一个 `visitX = 0`，节点 `accept`（4.1）；漏 override 编不过。Sema / codegen 仍走 `dynamic_cast`
- **修**：`SemaPass` 继承 `AstVisitor`，`visitExpr` / `visitStmt` 经 `accept` 分派（4.2）；漏节点编不过，不再静默 skip
- **修**：`Compiler` 继承 `AstVisitor`，`compileExpr` / `compileStatement` 经 `accept` 分派（4.3）；漏节点编不过，未知表达式 / 语句不再 `throwSemaGap`
- **修**：formatter `Printer` 继承 `AstVisitor`，表达式 / 语句经 `accept` 分派（4.4）；顶层 item 与类型节点仍走 parse tree（保住 `T?` / `T&`）；漏节点编不过
- **加**：薄 `PassManager`（5.1）：`addPass` + 分析表 Sema → fn checkers；`Compiler::compile` 再挂 codegen。`riu-check` / `Compiler` 只跑表，parse/build 不进表
- **修**：`compiler.h` 收成 LLVM + Module + codegen Pass driver（5.2）；ownership / destructor / dyn-vtable / types / call / expr / stmt 声明拆到窄头。新 codegen 成员写对应头，不必改 `compiler.h` 成员表
- **加**：全局 `#Cval` / `#Inline #Cval` 全面回归（同文件各宽度与常量表达式、诊断、跨文件读）；通配导入查找不再穿透他模块 `_` 前缀全局（链接期 undefined → E3030）

## 2026-09-17

- **加**：泛型 spec（#15）。`#Spec struct D<T>`、`#Impl(D<A>)`、`<X : D<A>>`；E1103 键 `(S, D)`；缺参 E1142；开 impl 按 struct 实参 subst 后再比。默认体 LLVM 签名会 subst；体 AST 的 resolved 槽仍可能是 spec 形参，项目测试未编默认体。回归：`diag_spec_generic_*`、`spec_generic_bound_ok`、`tests/projects/spec_generic`
- **加**：泛型 enum 3.1。`enumDecl` 接 `genericDef?`；`EnumDeclNode` 写入形参名与头上 `: D`（语义拒在 3.2）。回归：`enum_generic_parse_ok` / `enum_generic_bound_parse_ok`；现有 enum check-cases + SDK `riu.core.enum` / `riu.core.match`
- **加**：泛型 enum 3.2。使用点实参个数 E6011；头上 `<T : D>` → E2037；类型为名义名 + `genericArgs`，lookup 仍裸名。回归：`diag_enum_generic_*` / `enum_generic_type_ok`
- **加**：泛型 enum 3.3。构造 `E:<T>::V` / `E:<T>::V(payload)` 按该次实参 subst payload；TypeInfo 带 genericArgs；缺 turbofish / 非泛型写 `:<T>` → E6011。回归：`enum_generic_ctor_ok` / `diag_enum_generic_ctor_*`
- **加**：泛型 enum 3.4。match 穷尽按 variant 名；绑定类型按 scrut 的 `genericArgs` subst payload。回归：`enum_generic_match_ok` / `diag_enum_generic_match_*`
- **加**：泛型 enum 3.5。每单态一份 LLVM 类型（`generic::EnumInstance`）；拷贝 / 析构 tag dispatch 走 subst 后 payload。回归：`tests/projects/enum_generic`
- **加**：泛型 enum 3.6。`T ! E` 的 E 存 getFullName（`Box<String>` ≠ `Box<i32>`）；E7008 / 透传 / mangle / LLVM 包装按完整类型。回归：`enum_generic_fallible_ok` / `diag_enum_generic_fallible_*`；`tests/projects/enum_generic`
- **加**：泛型 enum 3.7。`.ud` v4 二进制 enum 写入 typeParams（lookup 仍裸名）；跨文件 `use` 后 `E<T>` 可从缓存加载。formatter 印 `enum E<T>`。回归：`tests/projects/enum_generic_cross`、`tests/projects/format` `format_enum_generic`
- **加**：泛型 enum 3.8。简单切片回写 §3.10 / 附录 B / D（E2037）；提案标已落地。方法 / `#Impl` / struct-style 仍后切片。
- **加**：类型别名 #17。`type Name = T` 关键字；块 / struct 作用域与 `let` 同；左侧无泛型；Reflect `type` → `type_info`。回归：现有 alias + `diag_alias_block_*` / `diag_alias_struct_*`；SDK `riu.core.alias` / `reflect`；原 `Pair<T> =` 改写为具体别名或元组；元组形参泛型推断仍需 turbofish
- **加**：rd.1 手写词法骨架。`riu/ast/rd` Token+Pos（Kind 对 g4）；`riu-ast --tokens` / `--rd-tokens`。Scanner 尚只吐 Eof。对照：`tests/rd-cases/smoke.*`
- **加**：rd.2 / rd.3 手写 Scanner。`;` 注释、关键字、Unicode ID、数字、`r"..."`、`c'...'`、`LineEnd`、StrTpl（`${` / `$id` / 括号深度）与 ANTLR default 通道对齐。`Pos` 用 code point。对照：`tests/rd-cases/`（`run.ps1`）；check-cases + SDK 全量 `--tokens`==`--rd-tokens`
- **加**：rd.4 FlatAst。`NodeKind` 覆盖 g4 顶层 item / 常用 expr / stmt / type；`riu-ast --rd` 缩进 dump。parser 未接，打空 `Program`。对照：`tests/rd-cases/empty.rd.txt`
- **加**：rd.5 Parser 骨架。`program` / `use typePath (.*)?` / 空行 / EOF；其余顶层跳过。`riu-ast --rd` 打 Use 树。对照：`tests/rd-cases/` `empty` / `blanks` / `use`
- **加**：rd.6 Pratt 表达式 + `fn` / `let` / `struct` / `enum` / `type` / 语句。`riu-ast --rd` 打声明与表达式树。对照：`tests/rd-cases/`（`./tests/rd-cases/run.ps1` 20 个）
- **加**：rd.7 Scanner/Parser 报 E1001/E1002；`syntax_diag` 抽出渲染/hint（ANTLR listener 只适配）。`parseProgram` 带错误表；`riu-ast --rd` 仍 dump。对照：`tests/rd-cases/` `illegal` / `bad_item` / `kw_param`；`diag_lexer_tab`；现有 `diag_*E100*`

## 2026-09-18

- **加**：rd.8 `RdBuilder`：FlatAst → FileNode；`riu_rd` 并进 `riu_ast`。`_parseFile` 走 rd（skeleton / format / LSP 仍 ANTLR）。对照：`tests/rd-cases/` 23；`riu-check test tests/check-cases/` 586/598（其余 rd.9）
- **加**：rd.9 切 `parseSkeletonInto` / `riu-check` 主路 rd；`parse_program` 调用点删掉。LetTuple RHS、tab E1001、item `sourceText` 含 `#Anno`、CName 去引号、诊断行落关键字、StringLit 仅 `r"..."` 为 raw。对照：check-cases 598；`sdk/riu` `riu test` 83；`./build.ps1 test` 80
- **修**：换行后 `.` / `?.` 按 g4 `LineEnd*` 续行，用 `.name =` 前瞻与 struct 字段断开，不再比列号。`let v = a\n    .b()` 不再 E1002。对照：`tests/rd-cases/chain`；`tests/check-cases/dot_chain_nl_ok`
- **修**：RdBuilder `tokenRange` / `tokenIndexAt` 对已排序 default token 二分，避免每节点从头扫（1M 行 check 从卡死级降到可跑）。对照：`dot_chain_nl_ok` + `mix_1m.ut` parse
- **修**：`FileNode` struct/fn/impl 按名 map（与 enum/alias 同）；Sema arity 跳过 `i32` 等基本类型。1M `riu-check` release 从分钟级 → **6.0 s**。对照：check-cases 599；`mix_check_1m.ut`
- **加**：rd.10 formatter 切 rd trivia + FileNode。`scanTrivia` 吃 `nextRaw()`；类型 `typeDocAst`；struct/spec/extern 仍原文。括号 lambda 的 pos 接到体末；`E::V()` 按源上 `(` 保留括号。对照：`tests/projects/format`
- **加**：rd.12 拆 ANTLR。删 `ast_builder*` visitor、`parse_program`、`riu/ast/gen/`、`gen-antlr.ps1`；`types.h` 去掉 antlr include；`riu-ast` 只 dump rd。对照：`./tests/rd-cases/run.ps1`；三套回归；`./lint.ps1` 0 warnings
- **加**：rd.13 语法权威换成 `riu/ast/riu.bnf`。删 `riuLexer.g4` / `riuParser.g4`；§1 / §2 / 附录 A/B / index 指针跟上。语言语义不变。
- **修**：清 ANTLR 残留。`Kind` 去掉未发射的 fragment；BUILD 不再编 `antlr4_static`；附录 D / 诊断 caret 按 UTF-8 字节列对齐 rd。对照：`./tests/rd-cases/run.ps1`
- **修**：`TypeInfo` intern 按身份挂槽（标量不再每节点一份）；`TypePath` 一段名 inline。

## 2026-09-19

- **加**：公开 `abort()`（`#NoReturn`）。`exit(code)` 仍是业务退出码；越界 / 空 `Nullable.get()` / OOM 改走 `riurt_abort`（SEH `0xE0AB0710`），不再 `ExitProcess(1)`。对照：`noreturn_expr_arm` / `sdk_public_surface`；`riu.core.block_value` `test_noreturn_arm_if_match_try`
- **加**：#2 2.1 `Indexed<T>`。SDK spec；for-in 在 Array / `[T*N]` 之后认 `#Impl(Indexed<U>)`，隐藏索引 loop，`item = U&`，入口拍 `len`。回归：`for_in_indexed_*` / `diag_for_in_not_indexed`、`tests/projects/for_in_indexed`、`control_flow.test` Indexed
- **加**：#2 2.2 `Map` `#Impl(Indexed<K>)` 加 `at`；`Set` 转发 `$._items.at(i)`；E3160 文案扩 Indexed。`for k in m` / `for x in s` 不再只走 `keys()`。回归：`map.test` / `set.test`
- **加**：#2 2.3 SDK `End` / `IterItem<T, E>` / `Iter<T, E>`；for-in 第三档 `#Impl(Iter<U, E>)` 每轮 match `next`（Item / End / Error）。`E = End` 的 for 不可失败；其它 E 须 try 或同型 `!`（E7006 / E7004）。回归：`for_in_iter_*` / `diag_for_in_iter_*`、`tests/projects/for_in_iter`、`control_flow.test` Iter
- **加**：#2 2.4 回写 §5.5.4 / §9.2.3.6 / §10.4.1.5 / §12.7.6 / 附录 C/D；提案标已落地。回归：`riu-check test tests/check-cases/` 609；`sdk/riu` `riu test` 83；`./build.ps1 test` 82
- **修**：P1 类型 intern。`Riu` 挂 intern 表；`getType` / `peelRef` / `peelAutoDeref` / `withoutFallible` 返回 `const TypeInfo&`；`TypeGenericNode` 与其它 TypeNode 同款缓存；intern 后只读。SDK 复用 FileNode 按所属 `Riu` intern，避免用户 intern 表析构后悬空。
- **修**：P2 去掉 `visitFn` 的 match 预扫。`visitMatch` 下钻臂体前填 payload；`structuralType`/`getType` 惰性填一次（已填不覆盖，避免 codegen subst 帧冲掉实例类型）。`enumInstSubst` 下沉到 `name_lookup`。
- **修**：P3 borrow + const-mut 合成 Sema 之后单趟 walk。同一节点先 borrow 再 const-mut；`#NoReturn` 仍按需。
- **修**：P4 Token 文本 intern。`TokenInfo` 只持 intern 指针；`makeTok` / 合成 token 进 `Riu` 的 `StringIntern`（与 RdBuilder 同寿）；建树前按源大小 `reserve`。
- **修**：P5 `SymbolInfo` / `FnSymbolInfo` 存 intern 类型指针；`ScopeNode` 局部表改 `unordered_map` + `unique_ptr`（rehash 不移动，保住 `ResolvedSymbol` 指针）。spec 默认体 emit 压 subst 帧。带类型 `let` 先 `visitExpr` 再 `getType`，match 块内无标注绑定可回填。


## 2026-09-20

- **加**：#20 `_` 丢弃名。恰好 `_` 只作字段 / 变量槽：可重复、不进符号表；字面量省略零填；禁止类型 / 函数 / 全局等（E3161）。`_foo` 仍私有。不改 bnf。回归：`discard_ok` / `diag_discard_*`、`tests/projects/discard`

## 2026-09-21

- **加**：#19 FFI 布局。`#Packed` / `#Align(N)`（struct 与字段）；`align_of<T>()`；`overlay:<U>(T&) U&`（C-layout、等 size、`align(U)≤align(T)`）。无语言 union。`extern` Win64 按布局后 size/align。回归：`layout_ok` / `diag_align_*` / `diag_overlay_*` / `diag_packed_*`、`layout.test`、`tests/projects/ffi_layout`
- **修**：`riu-check` 写 SDK `.ud` 时保留 spec/泛型 skeleton；缺原文则不写缓存，避免 v5 丢掉 `Eq` 默认体
- **加**：`sdk_loader` 加载目录包 `riu.platform.windows`（`platform/windows/` + `pkg`），不扁平进 core、不挂默认 `riu` 孩子。占位 `winver` 作加载探测。回归：`platform_windows_ok` / `diag_unimported_riu_platform_windows`、`tests/projects/sdk_windows_paths`
- **加**：本地 `scripts/win32` 从 Windows SDK 头生成 `riu.platform.windows`（clang x64、`WINVER=0x0A00`；宏→`#Cval #Inline`，struct→`#Packed`+`#Align`+`_` pad）。A 切片覆盖现有 helper/io/base 调用面；SDK 改 `use`，删 `helper.ut`。回归：`types.test.ut` size/align 对照 clang；三套回归
- **加**：Win32 生成 B 切片。头文件（file/process/memory/sync）∩ kernel32 导出，钉 `NTDDI_VERSION=0x0A00000D`（Win10 CU，不含 Win11）；`LARGE_INTEGER`/`OVERLAPPED`/`SYSTEM_INFO` 展开为 packed 视图。约 396 fn / 26 struct / 413 `#Cval`。core 不 `kernel32.*` 以免泄进默认 `riu.core`（`#Cval` 不能 `consts.NAME`，E5016；无返回 extern 点链 E3091）。回归：`platform_windows_kernel32_ok`、`types.test.ut`；三套回归
- **加**：#21 切片 1。声明位 `typeParam` 可 `= type`（bnf / rd）；AST 默认槽；`.ud` v6；E2041 尾部不连续、E2042 前向 / 成环。使用点补齐未做。回归：`generic_default` rd；`generic_default_decl_ok` / `diag_generic_default_*`
- **加**：#21 切片 2。使用点少写实参从左对齐、右侧按声明默认补齐（类型位 + turbofish）；调用点无 turbofish 仍先推断，失败槽再填。`Rc` 无默认仍 E6011。`.ud` 加载后 recache 接口类型。无 turbofish 的 `Self::factory` 不按默认 / 当前单态改返回类型。回归：`generic_default_ok` / `generic_impl_body_ok` / `diag_generic_default_arity` / `diag_generic_default_too_many`、`tests/projects/generic_default`
- **加**：#21 切片 3。`Ptr<T=()>`：`kindForBuiltinWrapper` / `hasGenericArgs` 认 `Ptr`；intern 一律 1 个 `genericArgs`（裸 `Ptr` ≡ `Ptr<()>`）；`null` 仍可赋任意 `Ptr<T>`。跨 `T` 不可赋。回归：`ptr_generic_ok` / `diag_ptr_generic_mismatch` / `diag_ptr_generic_arity` / `diag_ptr_typearg_ref`
- **加**：#21 切片 4。`ptr_of` → `Ptr<payload>`（`U&`/`Rc`/`Heap`/`Array`/`String`→`Ptr<u32>`）；`ptr_cast<T,U>`；`_ptr_*` 须 `Ptr<T>`（裸 `Ptr` 先 cast）；`Ptr<T>` 可宽化进 `Ptr<()>`；extern / `T&` 隐式只进擦除 `Ptr`。回归：`ptr_of_payload_ok` / `ptr_cast_ok` / `diag_ptr_of_erased_assign` / `diag_ptr_as_ref_erased`；`ptr_ffi.test` / `heap.test` / `tests/projects/ptr`；三套回归

## 2026-09-22

- **修**：`types.h` 分层拆分。`primitives` / `token` / `source_location` / `riu_error` / `type_info` / `type_validate`；`getFullName` / `substitute` / `operator==` / `fromFullName` / `internNamedType` 等迁入 `type_info.cpp`，`validate*` 迁入 `type_validate.cpp`；`types.h` 只作 umbrella（不含 `type_validate.h`，调用方按需 include）。改 `type_info.cpp` 只重编 1 TU；改校验逻辑约 9 TU（原 monolithic `types.h` 牵动 ~97）。`diagnostic.h` 不再拖完整 `TypeInfo`
- **加**：#21 切片 5。Win32 生成：`void*` 仍 `Ptr`；`DWORD*` / `LPCWSTR` → `Ptr<u32>` / `Ptr<u16>`；`HANDLE` 族 `struct _HANDLE {}` + `type HANDLE = Ptr<_HANDLE>`（typedef 链共用，DECLARE_HANDLE 拆开）。core `io` / `base` / `time` 调用点改 `ptr_of` / `ptr_cast` / `as_handle`。模块限定调用 `null` 可进 `Ptr<T>`；跨模块 struct 字段别名按声明模块展开布局；extern ABI 先展开别名（避免 HANDLE 走 i64）；`.ud` 按 UseSpec 补通配后再 recache 字段。回归：`win32_handle_ok` / `diag_win32_handle_*` / `ptr_null_module_call_ok`、`types.test.ut`；三套回归
- **加**：#21 切片 6。回写 §3.2.6 / §3.4.2.6 / §6.6.2–3 / §8.7.4 / §9.7.2 / 附录 C；提案标已落地
- **加**：#22 切片 1。`riu.toml` 多产物：顶层只留身份；`[library]` + `[[executable]]`；`external_links` 的 `//` / `./`（Windows 补 `.lib`，`dll` 复制到产出目录）。禁旧 `entry` / `[lib]` / `[link]`。SDK 声明 `//kernel32` `//shell32` 传到用户 exe，驱动不再写死。回归：`toml_*` expected_fail；迁仓库全部 toml。
- **加**：#22 切片 2。`riu build` 无参先编 `[library]`，再按声明顺序编全部 `[[executable]]`（仍不互链）。回归：`tests/projects/toml_multi_exe`
- **加**：#22 切片 3。`[library] type="dynamic"` 产出 `.dll` + 导入库 `.lib`（`/dll` `/noentry`）。链接时扫本库 obj 写带引号 `.def`（避开 lld `/EXPORT` 按逗号切选项），导出含 mangle 的符号。回归：`tests/projects/toml_dyn_lib`
- **加**：#22 回写。§10.1.1 / §10.1.2.1 / 附录 C/D；`docs/模块系统.md` / README / 入门文档跟上；提案标已落地
- **加**：#9 阶段 1。`[dependencies]` 解析（`sdk`/`path`/`git` 恰好一种；禁 `sdk="core"` / `dir` / 未知键）；path 图递归、同名不同路径 E5041、非库 E5038、键≠对方 name E5037。path 库编进调用方 `build/deps/<name>/`，不写对方 `build/`；`use` 登记 `lib_mod`（含传递）。回归：`toml_dep_*`、`dep_path`、`dep_path_conflict`
- **加**：#9 阶段 2。SDK 拆成 `sdk/core`（隐式 `riu.core.lib`）与 `sdk/stdlib`（`{ sdk = "stdlib" }` 才加载 `riu.lib`）；去掉 `extraSdkPackages`。core 的 println/args 改为本模块私有 Win32 声明，不依赖 platform。回归：`toml_dep_*` / `dep_path*`；`sdk/core` + `sdk/stdlib` 下 `riu test`；消费 `riu.io` / `riu.time` / `riu.platform` 的 examples 与 projects 加 stdlib 依赖。
- **加**：#9 阶段 3。`{ git, rev }` 完整 commit 检出到调用方 `build/dependences/<name>/`，产物仍 `build/deps/<name>/`。HEAD 已是该 rev 则跳过网络；工作区改过 E5043；clone/fetch/checkout 失败透传 git 输出与退出码；同名不同 url/rev E5041。回归：`dep_git` / `dep_git_bad_rev` / `dep_git_conflict` / `dep_git_dirty` / `toml_dep_git_rev` / `toml_dep_rev_format`
- **加**：#9 阶段 4 回写。§10.1.1 `[dependencies]` 与产物路径；§10.4 SDK 拆成 `sdk/core`（隐式）/ `sdk/stdlib`（显式）；删 §10.4.5.2「v1 不解析」。附录 C/D E5034–E5044；用户文档跟上；提案标已落地。

## 2026-09-23

- **加**：#23 切片 1。实例字段 `= expr` 留在 AST；声明处 const-eval（E3162）；字面量省略有默认字段，无默认仍 E3125；E3129 只计无默认有名字段。`#Val` / `#Frozen`、空 `Array<T> = []`、`#Cval` 引用。回归：`field_default_ok` / `diag_field_default_*`、`tests/projects/field_default`
- **加**：#23 切片 2。带默认的 struct 走 `.ud` skeleton（不升版本）；使用点再求一次并缓存。回归：`tests/projects/field_default_cross`
- **加**：#23 切片 3。回写 §7.1.1.6 / §7.3 / 附录 B/C/D / CHANGELOG；提案标已落地。对照：`field_default_ok` / `diag_field_default_*`、`tests/projects/field_default` / `field_default_cross`

## 2026-09-24

- **加**：构建脚本迁到 `src/riu_lang/`（`uv run`）；根目录仅保留 `sync-deps.ps1`，`build`/`lint`/`format`/`count-lines` 为生成的 `.cmd`/`.sh` 壳；`.venv` Python 3.12 供 GN。修 `sync-deps` 按名称过滤（`parse_args` 读 `sys.argv`）。
- **加**：`scripts/ps-sync-deps` → `64e810e`；`gn`/`uv` 只走 `DEPS.json`（`sync-deps.ps1` 缺 `bin/uv.exe` 时调 ps-sync-deps）；`utfcpp` 升至 `3d1ff6d`。
- **加**：除 `sync-deps.ps1` 外脚本全迁 Python（`test-projects`/`rd-cases`/prebuild.py）；删 `tests/run.ps1` 与各 `prebuild.ps1`。

## 2026-09-26

- **加**：#6 切片 1。`buildAnno` 扩多参/命名实参（`annoVal ::= expr`）；`letAnno` 并入 `buildAnno`；`parseAnno` 逗号分隔 + `name=expr`；`parseStatement` 去 let-only 限制，前缀注解挂任意语句。回归：`anno_multi` / `anno_if_expr` / `anno_stmt` / `anno_impl_str` / `anno_value_sugar`；`uv run rd-cases` 30/30。
- **加**：#6 切片 2。`AnnoCall`/`AnnoArg` AST；`Annotated` 迁 `_annoCalls`；`StatementNode::prefixAnnos`；`RdBuilder::buildAnnoCall`；formatter `annoCallDoc`；`#Impl` 括号内文本桥接 + 平衡 `<>` 时 `parseType`。回归：rd-cases 30/30、`riu-check test` 660/660。
- **加**：#6 切片 3。`builtin_annos.h` schema；`validateBuiltinAnnos`（附着 / 重复 / `#If` bool / `#CName` string）；const-eval 字符串字面量；`TypeInfo::fromFullName` 支持 `&`/`?`；`#Impl("…")` 全仓库迁移（含 `<>&?,` 实参）。回归：check-cases 660/660、rd-cases 30/30。
- **加**：#6 切片 4。Sema 前 `CfgStripPass` 摘 `#If` 假分支（真分支剥注解）；`#CName` 字符串实参去引号。回归：`if_cfg_*` / `diag_if_cfg_not_bool`、`tests/projects/if_cfg`；check-cases 668；SDK `riu test`；`./build test` 112。
- **加**：#6 切片 5。SDK `anno.ut`（`enum AnnoType` + `#Anno struct If/Impl/Align/CName/Anno`，不进 `pkg`）；回写 §11 / 附录 A/B/C/D / §7.8 / §12 / CHANGELOG；提案标已落地。编译器仍白名单。回归：`diag_sdk_anno_private`；check-cases 669；SDK `riu test`；`./build test` 112。
