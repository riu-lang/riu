# v0.23.0-alpha

进行中。目标约 2026-09-27。切版：三套测试全绿、`./lint.ps1` 0 warnings。

上新下旧。只写本文件。

## 2026-09-13

- **加**：`kBuiltinMethods` 从 `sema/` 迁到 `yux/include/`（编译器分离 1.1）；`file_node.cpp` 不再 include `sema/`
- **修**：E3030 拼写建议改在 Sema 重抛时附加（1.2）；`expr_node.cpp` / `literal_node.cpp` 不再 include `analyzer/`
- **加**：`resolveTypePath` 下沉到 `yux/ast/type_path.{h,cpp}`（1.3）；`type_node.cpp` 不再 include `sema/`；NameResolver 包一层给 Sema
- **修**：全局 `#Cval` / `#Mut` / val init 的 const-eval 从 builder 改到 SemaPass（1.4 选 b）；`ast_builder_decl.cpp` 不再 include `sema/`；E3140 随 Sema 抛
- **修**：match arm 绑定类型从 builder 改到 SemaPass（1.5）；`ast_builder_expr.cpp` 不再 include `sema/`
- **修**：`SpecRegistry` / `validateSpecImpls` 与 `Yux` 构造/析构从 `yux.cpp` 挪到 `analyzer/yux_spec.cpp`（1.6）；`yux.cpp` 不再 include `analyzer/`；`SemaPass::run` 起始处兜底校验
- **修**：结构叶 `getType()` 优先读 resolved 槽（1.7a）；Sema 先下钻再经 `type_of_struct` 写槽；泛型 codegen 用 `structuralType()` 无槽重算

## 2026-09-14

- **修**：Get / GetRef `getType()` 优先读 resolved 槽（1.7b）；Sema 经 `type_of_name` 写槽（Get 先下钻索引基）；GetRef 作用域查找仍在 `structuralType` 供无槽重算。codegen 不再给这两类节点填槽，避免 spec 默认体复用 AST 时锁死上次 impl 的类型
- **修**：Unary / AddSub / MulDiv / BinOp / Compare `getType()` 优先读 resolved 槽（1.7c）；Sema 先下钻再经 `type_of_ops` 写槽；运算符方法仍走 `tryValidate*`（call_resolve）。codegen 不再给这几类节点填槽
- **修**：Call / PathCall `getType()` 优先读 resolved 槽（1.7d）；Sema 先下钻再经 `type_of_call` 写槽；重载 / 内建方法 / #Static fn 仍走 Sema 分支（call_resolve / NameResolver）。codegen 不再填槽；subst 帧 `resolvedOrInferredType` 始终 `structuralType`+替换，避免复用模板 AST 时锁死上一实例
- **修**：Dot `getType()` 优先读 resolved 槽（1.7e）；Sema 先下钻基表达式再经 `type_of_dot` 写槽；字段 / 方法 / `?.` / Field.value 仍在 `structuralType`。方法点 E3095 仍由 Call 的 writeResolved 记下。灵活整数回填后 Dot 槽经 `structuralType` 重算。codegen 不再填槽
- **修**：IfElse / OneLineIfElse / Match / TryCatch / NullElse `getType()` 优先读 resolved 槽（1.7f）；Sema 先下钻再经 `type_of_ctrl` 写槽；汇合 / 流终止跳过 / `??` 剥 Nullable 仍在 `structuralType`。if/match/try 空 `[]` 靶向仍在 finishCtrlResolved。codegen 不再填槽
- **修**：Lambda / StructLit / DynCtor / MoveAssign `getType()` 优先读 resolved 槽（1.7g）；Sema 先下钻再经 `type_of_rest` 写槽；Fn 字面量 / Self·TypeName 字面量 / Dyn 包装 / `<-` 左侧仍在 `structuralType`。codegen 不再填槽
