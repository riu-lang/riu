; if_else 条件表达式用例
; 迁自 tests/cases/if_else.yux

#Test
fn test_if_max() {
  val a = 10
  val b = 20
  val max = if a > b { a } else { b }
  assert_eq(max, 20)
}

#Test
fn test_if_elif_else() {
  val score = 85
  val grade = if score >= 90 {
    1
  } elif score >= 80 {
    2
  } elif score >= 70 {
    3
  } else {
    4
  }
  assert_eq(grade, 2)
}

#Test
fn test_if_no_else_taken() {
  val a = 10
  var hit i32 = 0
  if a > 0 {
    hit = a
  }
  assert_eq(hit, 10)
}

; 迁自 tests/cases/inline_if.yux —— 后缀条件式 `<expr> if <cond> else <expr>`

#Test
fn test_postfix_if_else_max() {
  val a = 10
  val b = 20
  val m = a if a > b else b
  assert_eq(m, 20)
}

#Test
fn test_postfix_if_else_sign_pos() {
  val a = 10
  val sign = 1 if a >= 0 else -1
  assert_eq(sign, 1)
}

#Test
fn test_postfix_if_else_sign_neg() {
  val a = -3
  val sign = 1 if a >= 0 else -1
  assert_eq(sign, -1)
}

#Test
fn test_postfix_if_else_string() {
  val a = 10
  val msg = "positive" if a > 0 else "non-positive"
  assert_eq(msg, "positive")
}
