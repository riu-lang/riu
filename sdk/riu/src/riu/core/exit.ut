; riu SDK - 公开 exit(code u32) stdlib 函数（DRAFT-错误.md §8.7 / spec §11.5.1）
;
; #NoReturn 注解：编译器流终止分析复用（10d-2）。
; 直接调 Win32 ExitProcess（已标 #NoReturn），不经 _exit 包装：u32 与 ExitProcess
; 形参类型一致，省一次 to_i32() / to_u32() 来回。

use riu.core.helper.*

#NoReturn
fn exit(code u32) {
  ExitProcess(code);
}
