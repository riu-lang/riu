; 进程：args() / tick_ms()
; args 在 riu test 里是 riu-test-runner 的命令行，只断言非空。

#Test
fn test_args_nonempty() {
  let a Array<String> = args()
  assert_true(a.len() >= 1)
  assert_true(a[0].len() > 0)
}

#Test
fn test_tick_ms_runs() {
  let t u32 = tick_ms()
  assert_true(t == t)
}